{
  "name": "workflows-worker",
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker runtime for routing and retrying background workflows.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aashahin/cloudflare-workflows-worker.git"
  },
  "homepage": "https://github.com/aashahin/cloudflare-workflows-worker#readme",
  "keywords": [
    "cloudflare",
    "cloudflare-workers",
    "cloudflare-workflows",
    "worker",
    "workflow",
    "queues",
    "background-jobs"
  ],
  "scripts": {
    "build:cf:production": "wrangler deploy --dry-run --config wrangler.jsonc --env=\"\"",
    "build:cf:sandbox": "wrangler deploy --dry-run --config wrangler.jsonc --env sandbox",
    "dev": "wrangler dev",
    "deploy:production": "wrangler deploy --config wrangler.jsonc --env=\"\"",
    "deploy:sandbox": "wrangler deploy --config wrangler.jsonc --env sandbox",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@abshahin/workflows-sdk": "^0.1.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.75.0"
  }
}
